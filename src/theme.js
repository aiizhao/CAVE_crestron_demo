import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      white: '#ffffff',
      light: '#d8d3db',
      background: '#beb7c4',
      main: '#534a5c',
      dark: '#423350',
    },
  },
})

export default theme
